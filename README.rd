You can run the application in Eclipce with run as Java Application if you have Maven plugin (apache-maven-3.3.3)
Or you can use these commands with apache-maven-3.3.3, while you are in the project folder :
mvn clean install
mvn spring-boot:run